{% extends "Task.html.twig" %}

{% block title %}Gesdon &bull; Import Paypal{% endblock %}

            {% block content %}

                <div class="page-header">
                    <h1>Importer les données de Paypal</h1>
                </div>
                <div class="alert alert-info">
                    <i class="glyphicon glyphicon-bullhorn"></i>
                    Le fichier doit être issu de l'export CSV de Paypal (virgule et guillemet)
                </div>
                <div class="row">
                    <div class="col-md-12">
                        Sélectionnez un fichier CSV à importer pour Paypal
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-12">
                        <form action="{{ url('task_paypal') }}" method="post" role="form" {{ form_enctype(form) }}>
                            <div class="form-group">
                                <label for="form_file">Fichier CSV</label>
                                {{ form_widget(form.file, {class: "form-control"}) }}
                            </div>
                            <div class="form-group">
                                <button type="submit" name="import" class="btn btn-success">
                                    <i class="glyphicon glyphicon-import"></i>
                                    Importer
                                </button>
                            </div>
                            {{ form_rest(form) }}
                        </form>
                    </div>
                </div>
            {% endblock %}